@model AppUnipsico.Models.Consulta

@{
    ViewData["Title"] = "Delete";
}

<div class="container mt-4">
    <div class="row justify-content-center">
        <div class="col-md-8">
            <div class="card">
                <div class="card-body">
                    <h4 class="card-title">Deletar Consulta</h4>

                    <p class="lead">Tem certeza de que deseja remover esta consulta?</p>

                    <dl class="row">
                        <dt class="col-sm-4">Data da Consulta:</dt>
                        <dd class="col-sm-8">@Html.DisplayFor(model => model.DataConsulta.Data)</dd>

                        <dt class="col-sm-4">Status:</dt>
                        <dd class="col-sm-8">@Html.DisplayFor(model => model.StatusConsulta)</dd>

                        <dt class="col-sm-4">ID do Usuário:</dt>
                        <dd class="col-sm-8">@Html.DisplayFor(model => model.UsuarioId)</dd>

                        <dt class="col-sm-4">Nome do Usuário:</dt>
                        <dd class="col-sm-8">@Html.DisplayFor(model => model.Usuario.UserName)</dd>
                    </dl>

                    <form asp-action="Delete" method="post">
                        <input type="hidden" asp-for="ConsultaId" />
                        <div class="text-center">
                            <button type="submit" class="btn btn-danger mr-2">Deletar</button>
                            <a class="btn btn-secondary" asp-action="Index">Cancelar</a>
                        </div>
                    </form>
                </div>
            </div>
        </div>
    </div>
</div>
